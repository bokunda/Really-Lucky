using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace ReallyLuckyBackend.Helpers
{
    public static class Enums
    {
        public enum Color { WHITE, GREY, BLACK, REG, GREEN, BLUE, ORANGE };
        public enum TicketStatus { WAITING, LOSE, WIN };
    }
}
